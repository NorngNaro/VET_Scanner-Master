<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".activities.changeDestination.ReturnMenuActivity">

    <include layout="@layout/app_bar" />

    <LinearLayout
        android:gravity="center"
        android:layout_margin="20dp"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <Button
            android:onClick="returnOrigin"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:background="@color/colorAccent"
            android:text="Return to Origin ត្រលប់ទៅសាខាដើម"
            android:enabled="true"
            android:textColor="@color/white" />
        <Button
            android:onClick="returnHQ"
            android:id="@+id/button_save"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_marginTop="10dp"
            android:background="@color/colorAccent"
            android:text="Return to HQ ត្រលប់ទៅសាខាកណ្តាល"
            android:enabled="true"
            android:textColor="@color/white" />

    </LinearLayout>
</LinearLayout>